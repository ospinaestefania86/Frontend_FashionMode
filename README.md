# **Frontend Fashionmode**

Este proyecto fue generado utilizando **Angular CLI** versión **20.1.4**.

---

## **Ramas del proyecto**
- **`trabajo_1`** → Contiene el desarrollo correspondiente al **Trabajo 1**, utilizando Angular conectado a la API pública DummyJSON.
- **`trabajo_2`** → Contiene el desarrollo correspondiente al **Trabajo 2**, en el que el frontend está conectado a un **backend propio en Laravel**.

📌 Para ejecutar el **Trabajo 2** es necesario tener corriendo el backend disponible en el siguiente repositorio:  
[Backend FashionMode](https://github.com/ospinaestefania86/Backend_FashionMode)

---

## **Requisitos previos**
Antes de comenzar, asegúrate de tener instalado:
- **Node.js** (versión recomendada: 18 o superior)
- **Angular CLI** (versión 20.1.4 o compatible)
- **Git**

---

## **Instalación del proyecto**
1. Clonar este repositorio:
   ```bash
   git clone https://github.com/ospinaestefania86/Frontend_FashionMode.git
   ```
2. Cambiar a la rama deseada:
   - Para el Trabajo 1:
     ```bash
     git checkout trabajo_1
     ```
   - Para el Trabajo 2:
     ```bash
     git checkout trabajo_2
     ```
3. Instalar dependencias:
   ```bash
   npm install
   ```

---

## **Ejecución del servidor de desarrollo**
Para iniciar el servidor local:
```bash
ng serve
```
Una vez ejecutado, abre tu navegador en:  
[http://localhost:4200/](http://localhost:4200/)  
La aplicación se recargará automáticamente cada vez que realices cambios en los archivos fuente.

---

## **Generar componentes con Angular CLI**
Para crear un nuevo componente:
```bash
ng generate component nombre-del-componente
```
Para ver todas las opciones disponibles:
```bash
ng generate --help
```

---

## **Compilación para producción**
Para compilar el proyecto:
```bash
ng build
```
Los artefactos generados se almacenarán en el directorio `dist/`.  
Por defecto, la compilación de producción optimiza la aplicación para mejorar rendimiento y velocidad.

---

## **Recursos adicionales**
Para más información sobre Angular CLI y sus comandos:  
[Angular CLI - Documentación Oficial](https://angular.io/cli)
